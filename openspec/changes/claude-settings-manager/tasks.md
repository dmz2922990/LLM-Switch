## 1. 项目初始化

- [x] 1.1 使用 `create-tauri-app` 初始化 Tauri v2 + React + TypeScript 项目骨架
- [x] 1.2 配置前端依赖：React Router、Monaco Editor (`@monaco-editor/react`)、UI 组件库
- [x] 1.3 配置 Rust 后端依赖：`sqlx`（SQLite）、`ssh2`（SSH/SCP）、`aes-gcm`（加密）、`keyring`（系统密钥管理器）
- [x] 1.4 配置跨平台构建脚本（macOS / Linux / Windows）

## 2. 数据库与存储层

- [x] 2.1 创建 SQLite 数据库初始化逻辑，定义 `profiles`、`hosts`、`sync_history` 三张表的 schema
- [x] 2.2 实现 `profiles` 表的 CRUD 操作（创建、读取、更新、删除）
- [x] 2.3 实现 `hosts` 表的 CRUD 操作
- [x] 2.4 实现 `sync_history` 表的写入和查询操作
- [x] 2.5 实现数据库迁移机制，确保后续版本可平滑升级表结构

## 3. 凭据加密模块

- [x] 3.1 实现 AES-256-GCM 加密/解密函数，用于密码字段的编解码
- [x] 3.2 实现系统密钥管理器集成（macOS Keychain / Linux Secret Service / Windows Credential Manager），获取/存储加密主密钥
- [x] 3.3 实现密钥管理器不可用时的回退方案：应用级主密码机制
- [ ] 3.4 编写加密模块单元测试，验证加密/解密正确性和密钥管理器回退逻辑

## 4. Rust 后端服务 — 配置档案

- [x] 4.1 实现 Profile Service：创建档案（支持空模板和导入文件两种方式）
- [x] 4.2 实现档案列表查询（返回名称、时间、活跃状态）
- [x] 4.3 实现活跃档案切换：将档案内容写入 Claude Code `settings.json`，处理跨平台路径定位
- [x] 4.4 实现档案重命名（含名称唯一性校验）
- [x] 4.5 实现档案复制（自动生成 "副本"、"副本 2" 等名称）
- [x] 4.6 实现档案删除（含活跃档案的额外确认逻辑）
- [x] 4.7 实现写入失败时的回滚机制（保持原活跃档案不变）
- [x] 4.8 注册所有 Profile 相关 Tauri IPC 命令

## 5. 前端 — 配置档案管理界面

- [x] 5.1 实现应用主布局（侧边栏档案列表 + 主内容区）
- [x] 5.2 实现档案列表组件：展示所有档案，高亮活跃档案，显示空状态引导
- [x] 5.3 实现"新建档案"对话框：支持空模板和导入文件两种模式
- [x] 5.4 实现"切换"按钮交互：调用后端切换活跃档案，更新 UI 状态
- [x] 5.5 实现"重命名"内联编辑交互，含重复名称校验提示
- [x] 5.6 实现"复制"按钮，自动生成副本名称并刷新列表
- [x] 5.7 实现"删除"按钮，含确认对话框和活跃档案警告

## 6. 前端 — JSON 编辑器

- [x] 6.1 集成 Monaco Editor 组件，配置 JSON 语法高亮和自动补全
- [x] 6.2 实现编辑器加载档案内容、保存修改（含 JSON 格式校验）
- [x] 6.3 实现 JSON 格式化按钮功能
- [x] 6.4 实现编辑活跃档案保存后自动同步到 settings.json
- [x] 6.5 实现未保存修改的关闭确认对话框
- [x] 6.6 实现格式错误时的高亮标记和错误提示

## 7. Rust 后端服务 — 主机连接管理

- [x] 7.1 实现 Host Service：添加主机（密码认证和密钥认证两种方式）
- [x] 7.2 实现主机列表查询（返回名称、地址、端口、最后同步时间）
- [x] 7.3 实现主机信息编辑（含密码重新加密存储）
- [x] 7.4 实现主机删除（同步删除关联的 sync_history 记录）
- [x] 7.5 实现 SSH 连接测试功能：尝试建立连接并返回成功/失败结果
- [x] 7.6 注册所有 Host 相关 Tauri IPC 命令

## 8. 前端 — 主机连接管理界面

- [x] 8.1 实现远程主机管理面板布局（主机列表 + 添加/编辑表单）
- [x] 8.2 实现"添加主机"表单：名称、地址、端口、用户名、认证方式选择
- [x] 8.3 实现密码输入组件（密码和密钥路径切换）
- [x] 8.4 实现"测试连接"按钮，显示连接结果
- [x] 8.5 实现主机列表组件，展示所有主机信息
- [x] 8.6 实现主机编辑和删除交互

## 9. Rust 后端服务 — 远程同步

- [x] 9.1 实现 SSH/SCP 文件传输核心逻辑：连接远程主机并上传文件到指定路径
- [x] 9.2 实现单台主机同步：读取档案内容 → SSH 连接 → SCP 上传 → 记录同步历史
- [x] 9.3 实现批量主机同步：并行推送配置到多台主机，汇总各主机结果
- [x] 9.4 实现同步错误处理：区分连接超时、认证失败、路径不存在等错误类型
- [x] 9.5 实现同步历史查询接口
- [x] 9.6 注册所有 Sync 相关 Tauri IPC 命令

## 10. 前端 — 远程同步界面

- [x] 10.1 实现同步面板：选择档案 + 选择一台或多台主机
- [x] 10.2 实现"同步"按钮，显示同步进度和结果
- [x] 10.3 实现批量同步结果汇总视图（每台主机的成功/失败状态）
- [x] 10.4 实现同步历史记录面板（按时间倒序展示）
- [x] 10.5 实现远程路径配置（每台主机的自定义目标路径）

## 11. 系统托盘

- [x] 11.1 实现系统托盘图标和基础菜单
- [x] 11.2 实现托盘右键菜单：列出所有档案，标记当前活跃档案（勾选标记）
- [x] 11.3 实现从托盘菜单切换活跃档案（无需打开主窗口）
- [x] 11.4 实现托盘菜单"打开主窗口"和"退出"选项

## 12. 测试与打包

- [ ] 12.1 编写 Profile Service 的后端单元测试（CRUD、切换、复制、重命名）
- [ ] 12.2 编写加密模块集成测试（加密存储 → 解密使用完整流程）
- [ ] 12.3 编写 SSH 同步模块的模拟测试（使用 mock SSH server）
- [ ] 12.4 macOS 平台构建与基础功能验证
- [ ] 12.5 Linux 平台构建与基础功能验证
- [ ] 12.6 Windows 平台构建与基础功能验证
